[pytest]
pythonpath = .

markers =
    opensearch_integration: requires OpenSearch running (search e2e / bootstrap)
    ingest_integration: integration tests (ingest)
    transcribe_integration: integration tests (transcribe)
    transcription_integration: transcription worker integration tests
    integration: generic integration tests (requires external services)
