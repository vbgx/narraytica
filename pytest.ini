[pytest]
pythonpath = .

markers =
    opensearch_integration: requires OpenSearch running
    ingest_integration: ingest integration tests (MinIO + Postgres)
    transcribe_integration: transcribe integration tests
    transcription_integration: transcription worker integration tests
    integration: generic integration tests requiring external services
