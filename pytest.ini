[pytest]
pythonpath = .

markers =
    transcribe_integration: transcribe worker integration tests
    transcription_integration: transcription worker integration tests
    integration: integration tests (requires Postgres + MinIO)
    ingest_integration: integration tests (ingest)
